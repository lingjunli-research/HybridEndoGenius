universe        = vanilla
executable      = casanovo.sh

arguments       = $(mgf_file) $(yaml_file) $(ckpt_file) $(out_dir) $(sample)

# List all required input files for the job
transfer_input_files = casanovo.sh, $(yaml_file), $(ckpt_file), $(mgf_file)

# Output and error logs for monitoring
output          = $(out_dir)/logs/casanovo_t_$(Cluster)_$(Process).out
error           = $(out_dir)/logs/casanovo_t_$(Cluster)_$(Process).err
log             = $(out_dir)/logs/casanovo_t_$(Cluster).log

# Remap the output file.
transfer_output_files = $(sample).mztab
transfer_output_remaps = "$(sample).mztab = $(out_dir)/Casanovo/$(sample).mztab"

# Resource requests
request_cpus    = 6
request_memory  = 32GB
request_disk    = 40GB

# Submit one job
queue
